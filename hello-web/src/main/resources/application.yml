server:
  port: 8080
  servlet:
    context-path: /hello


spring:
  profiles:
    include: sharding

  redis:
    host: 127.0.0.1
    port: 6379
    password: Zdk@2020
    database: 0

mybatis-plus:
  mapper-locations: classpath*:config/**/mapper/*.xml
  type-handlers-package: com.zdk.hello.handler

logging:
  level: 
    com: debug